// Configuration for Telegram bot:
telegram = 
{
  // Telegram python API parameters:
  api_token =     "TOKEN";
  poll_interval = 0.0;
  timeout =       10.0;
  bootstrap_retries = -1;

  // Name of our bot:
  bot_name = "AWESOME-BOT";

  // Chat-/User-IDs which are allowed to send commands to
  // this bot:
  authorized_ids = [ID1, ID2, IDn];

  // Chat-/User-IDs which will be notified whenever the
  // system broadcasts messages (infos, warnings or errors)
  broadcast_ids = [ID1, ID2];
};


